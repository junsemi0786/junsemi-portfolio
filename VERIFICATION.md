# 검증 기록 · 2026-09-11

대상: GitHub `junsemi0786/junsemi-portfolio`를 내려받아 만든 로컬 `codex/b2b-renewal` 브랜치. 운영 사이트와 운영 Redis에는 쓰기 작업을 하지 않았습니다.

- `npm run build`: Next.js 16.1.4 배포용 빌드 및 TypeScript 통과.
- `npm run lint`: 오류·경고 없이 통과. 원본에 빠진 의존성 2종을 package-lock.json에 반영하여 설치 불일치를 해결했습니다.
- `node --test tests/session.test.mjs`: 위조·만료·비밀번호 변경 세션 거부, 기본 비밀번호 인증 차단, 기존 테스트 문구 정리 검증.
- `tests/site_smoke.py`: 배포용 서버의 공개 페이지 26개, 내부 링크 55개, canonical·제목·설명·JSON-LD, 사이트맵·RSS 검사 통과. 공개 프로젝트 7건만 노출, 기존 Mercury 초안은 직접 URL에서도 404. 위조 관리자 쿠키로 보호된 화면에 진입할 수 없음.
- CUA 브라우저: 355px 모바일과 1280px 데스크톱에서 홈 화면·메뉴·이메일 초안·프로젝트 필터 확인. 가로 넘침과 Next.js 오류 화면 없음. 실제 이메일 전송은 하지 않았습니다.
- 프로젝트 편집: 별도 로컬 저장소에서 관리자 로그인 → 새 초안 저장 → 공개 → 내용 유지 확인 → 초안 전환 → 삭제를 수행했습니다. 공개 시 목록·상세·사이트맵·RSS에 반영되고 비공개 전환 시 제거됨을 확인했습니다. 정리 후 원본 8건과 데이터가 동일함을 비교했습니다.

원본 실적의 ID·사진·공개 상태를 보존했습니다. Redis 연결 시 기존 키를 사용하도록 구현했으나 운영 Redis 연결·저장은 이번 로컬 검증에 포함되지 않습니다. 배포 후 기존 로그인과 실적 편집 연결을 확인해야 합니다. 로컬 편집용 파일 저장은 개발 모드에서만 명시적으로 켤 수 있습니다.

검색은 Vercel Production에서 허용하며 Preview·개발에서는 제외합니다. 구글·네이버의 사이트 소유 확인 및 사이트맵 제출은 아직 하지 않았습니다.

추가 방문자 카운터: 기존 Visitor Badge 이미지가 실제로 로드되고 숫자가 표시됨을 브라우저에서 확인했습니다. 모바일 하단에서 가로 넘침 없이 표시됩니다. 미리보기 집계 키와 운영 집계 키를 분리했습니다.
